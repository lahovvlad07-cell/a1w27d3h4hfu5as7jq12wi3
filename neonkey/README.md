# NeonKey — сайт цифровых товаров (v3.2)

Двухстраничный сайт: публичный лендинг **и отдельный личный кабинет**
за входом (каталог, профиль с аватаром, история заказов, информация о
проекте) — как это и должно быть, каталог не живёт на главной.

## Что изменилось в 3.2 (по итогам ревью)

- ✅ **Каталог убран с главной страницы полностью.** `index.html` — это
  теперь чистый лендинг: hero, «как это работает», футер. Ни одной
  кнопки «Купить» на главной больше нет.
- ✅ **Появилась вторая страница — `account.html`, личный кабинет.**
  Доступна только после входа (без сессии показывается экран
  «Войдите, чтобы продолжить»). Внутри — три вкладки:
  - **Каталог** — то, что раньше было на главной.
  - **Профиль** — аватар (клик по кружку открывает выбор из 16 эмодзи),
    имя/почта, статус способов входа (Email/Telegram, с кнопкой
    «Привязать» для добавления Telegram к аккаунту, вошедшему по email),
    последние 5 заказов, кнопка «Выйти».
  - **Информация** — «О проекте», FAQ, Политика конфиденциальности,
    Публичная оферта — тот же контент, что раньше был в Mini App.
- ✅ Кнопки «Открыть личный кабинет» на лендинге либо сразу ведут в
  `account.html` (если уже вошли), либо сначала открывают модалку
  входа и уже потом ведут в кабинет.

## Структура проекта

```
webapp/                    сайт — единственное, что нужно деплоить
  index.html                 лендинг: hero, «как это работает», футер — без каталога
  account.html                личный кабинет (за входом): каталог + профиль + информация
  assets/
    logo.png                  реальное лого бренда — brand-mark и favicon
  css/
    tokens.css                 цвет (палитра снята с лого), типографика, отступы
    base.css                   сброс, фон, типографика, focus-стили
    components.css              кнопки, навигация, модалка входа, тост, бейджи
    sections.css                 hero, шаги, стили карточек каталога (общие для account.html)
    account.css                   вкладки кабинета, профиль, аватар, инфо (только account.html)
    animations.css               keyframes, scroll-reveal, hero-последовательность
  js/
    config.js                   ключи Supabase, ссылки, юзернейм бота, список аватаров
    state.js                    состояние (текущий пользователь)
    data/catalog.js              список товаров, цена за штуку/комиссия, лимиты количества
    lib/
      supabaseClient.js
      telegramAuth.js            Telegram Login Widget + серверная проверка входа
      auth.js                     email/пароль, updateUserMetadata (для аватара и т.п.)
      orders.js                   тихий лог последних 5 заказов (для будущего Mini App)
      scrollReveal.js              IntersectionObserver-обёртка для анимаций
    ui/
      authModal.js                 модалка входа (вкладки Telegram / email) — общая для обеих страниц
      headerAuthState.js            «Войти» ↔ ссылка в кабинет в шапке лендинга
      catalogView.js                 рендер каталога, стрелки количества/суммы, кнопка «Купить» (только на account.html)
      catalogIcons.js                 SVG-иконки товаров под палитру бренда
      checkoutModal.js               окно оплаты (iframe + «открыть в новой вкладке»)
      telegramLinkModal.js            модалка «Привязать Telegram» (для входа по email)
      avatarPicker.js                 модалка выбора аватара
      profilePage.js                  вкладка «Профиль»
      infoTabs.js                     вкладка «Информация» (About/FAQ/Политика/Оферта)
      docsModal.js                    модалка Политика/Оферта (используется на лендинге, в футере)
      toast.js
    content/legalDocs.js             тексты Политики и Оферты (шаблон, показать юристу)
    main.js                           точка входа для index.html
    account.js                        точка входа для account.html

supabase/
  functions/
    _shared/telegram.ts            проверка подписи Telegram Login Widget
    telegram-auth/                  вход/регистрация через Telegram
    telegram-link/                  привязка Telegram к текущему аккаунту
    link-email/                     привязка/переподвязка email к Telegram-аккаунту
bot/
  main.py, config.py, keyboards.py, handlers/start.py
  (Telegram-бот — точка входа в будущий Mini App; сайта не касается)
```

`supabase/` и `bot/` — общий бэкенд для сайта и будущего Mini App,
поэтому оставлены как есть.

## Синхронизация с будущим Mini App

Когда будешь собирать Mini App отдельным проектом, чтобы данные не
разъехались:

1. **Один и тот же Supabase-проект.** Возьми `SUPABASE_URL` и
   `SUPABASE_ANON_KEY` из `webapp/js/config.js` — тогда вход (Telegram
   или email) и сайт создают/находят один и тот же аккаунт.
2. **Один и тот же каталог.** Держи `webapp/js/data/catalog.js`
   синхронным с каталогом в Mini App (те же `id`, `checkoutUrl`, цены).
3. **Формат истории заказов.** Сайт пишет последние 5 заказов в
   `user_metadata.orders` аккаунта (см. `js/lib/orders.js`) в простом
   формате `{ id, name, icon, price, date }`. Mini App может читать это
   же поле — не нужна отдельная таблица.
4. **Палитра.** Токены в `webapp/css/tokens.css` (голубой/сиреневый/
   розовый на почти чёрном, снято с `assets/logo.png`) — используй те же
   переменные и в Mini App, чтобы не было ощущения двух разных продуктов.
5. **Структура.** Mini App по смыслу ближе к `account.html` (кабинет),
   чем к `index.html` (лендинг) — в Telegram люди сразу открывают
   каталог/профиль, вступительный экран там не нужен.

## Что нужно донастроить перед запуском

### 1. Цена, комиссия и ссылки на оплату товаров
Цена уже посчитана прямо в каталоге, отдельно настраивать не нужно:
- **Telegram Stars** — `pricePerUnit: 1.49` (₽ за одну звезду) в
  `webapp/js/data/catalog.js`; покупатель стрелками выбирает количество,
  итог считается на карточке.
- **Пополнение Steam** — `feePercent: 6`; покупатель вводит сумму
  пополнения, к оплате идёт сумма + комиссия.

Обе цифры и лимиты (`min`/`max`/`step`) правятся прямо в объекте товара
в `data/catalog.js`.

Кнопка «Купить» уже работает и без платёжной ссылки — пока `checkoutUrl`
не заведён, она честно открывает Telegram-бота с посчитанной суммой в
параметре `start`, чтобы заказ можно было принять и оплатить вручную (на
карточке при этом показана короткая пометка об этом). Когда платёжная
страница готова:
1. Заведи товар в своей платёжной системе с ценой, уже включающей
   комиссию/наценку, и получи ссылку на страницу оплаты.
2. Вставь ссылку в `webapp/js/data/catalog.js`, поле `checkoutUrl`, для
   нужного товара — кнопка «Купить» сама переключится на встроенное окно
   оплаты (см. ниже) вместо перехода в бота.

**Важно:** некоторые платёжные страницы запрещают показ себя во фрейме
(X-Frame-Options/CSP) — тогда покупатель нажмёт «Открыть в новой
вкладке» в окне оплаты. Это нормально, проверяется на реальных товарах.

### 2. Вход через Telegram и привязка аккаунта — требуют одноразовой настройки на сервере
Email/пароль работает «из коробки» (это штатная функция Supabase Auth).
А вот вход через Telegram, привязка Telegram к уже созданному
email-аккаунту («Привязать» у Telegram в профиле) и привязка email к
аккаунту, созданному через Telegram («Привязать» у email в профиле),
проверяются не в браузере (токен бота и service-role ключ нельзя
доверять клиентскому коду), а в трёх Supabase Edge Functions — **пока
они не задеплоены, соответствующие кнопки будут закономерно выдавать
ошибку связи с сервером**. Это не баг фронтенда, а недостающий шаг
инфраструктуры:

1. **Домен сайта.** У @BotFather → выбери бота → `/setdomain` → укажи
   домен, на котором будет висеть `webapp/`. Без этого виджет Telegram
   Login не заработает — ни для входа, ни для привязки.
2. **Деплой Edge Functions.**
   ```bash
   supabase functions deploy telegram-auth
   supabase functions deploy telegram-link
   supabase functions deploy link-email
   ```
   Без CLI (прямо на сайте supabase.com → Edge Functions → Create a new
   function) для каждой функции нужен ОДИН файл без импортов — веб-редактор
   не поддерживает импорт соседних файлов вроде `_shared/telegram.ts`.
   Для этого рядом лежат готовые самодостаточные версии — вставляй их:
   `telegram-auth/index.dashboard.ts`, `telegram-link/index.dashboard.ts`
   и `link-email/index.dashboard.ts` (весь нужный код уже встроен внутрь,
   копировать больше ничего не нужно).
3. **Секреты функций.** В Supabase Dashboard → Edge Functions → Secrets
   пропиши:
   - `BOT_TOKEN` — тот же токен, что в `bot/config.py`/`.env` (нужен
     `telegram-auth` и `telegram-link`).
   - `SUPABASE_SERVICE_ROLE_KEY` — Settings → API → service_role key
     (⚠️ секретный ключ, никогда не должен попасть в `webapp/`; нужен
     всем трём функциям).
4. **Конфиг фронтенда.** В `webapp/js/config.js` проверь `BOT_USERNAME` —
   юзернейм бота без `@`.

**Почему у email отдельная функция, а не обычный `auth.updateUser()`:**
у Supabase по умолчанию включена «Secure email change» — смена email
требует перехода по ссылке из письма, отправленного не только на новый
адрес, но и на старый. У Telegram-аккаунтов «старый» адрес — служебный
`tg-<id>@telegram.neonkey.app` (см. `_shared/telegram.ts`), который
никогда не был настоящим почтовым ящиком, поэтому подтвердить его
Supabase не может — вся операция падает с ошибкой вида `Email address
"tg-...@telegram.neonkey.app" is invalid`. `link-email` меняет email
напрямую через admin API (в обход подтверждения по старому адресу) и
сразу помечает новый адрес подтверждённым.

Пока не задеплоено — просто говори покупателям входить по email, это
не блокирует запуск сайта.

### 3. Bot
Токен бота в `bot/config.py` — тестовый, перед публичным запуском
обязательно отзови его через @BotFather → `/revoke` и впиши новый в
`.env` (см. `bot/.env.example`). `SHOP_URL` бота стоит направить на
будущий Mini App, когда он будет готов.

## Честная оговорка про историю заказов

У сайта нет своей таблицы заказов и нет вебхука от платёжной системы,
поэтому запись в `user_metadata.orders` — это лог момента, когда
пользователь нажал «Купить» и открылось окно оплаты, а не подтверждение
того, что оплата прошла успешно. Настоящий статус заказа видит только
платёжная система. Если понадобится показывать реальный статус оплаты —
нужен вебхук от провайдера и отдельная запись о заказе.

## Локальный запуск

`webapp/` — статические файлы, достаточно любого HTTP-сервера:
```bash
npx serve webapp
```
или деплой на Vercel/Netlify/Cloudflare Pages — просто указывай
`webapp/` как корень сайта.

`bot/` — Python 3.11+, `pip install -r requirements.txt`, затем
`python main.py`.
