# NeonKey — сайт цифровых товаров (v3.1)

Публичный сайт для продажи цифровых товаров (Steam, Telegram Stars и
т.д.): лендинг, каталог, вход через Telegram или email — покупка
открывается сразу после входа.

## Что изменилось в 3.1 (по итогам ревью)

- ✅ **Каталог теперь за входом.** Ассортимент виден всем (это лучше
  для SEO и понимания, что вы продаёте), но кнопки «Купить»
  заблокированы (карточки размыты, есть оверлей «Войдите, чтобы
  купить»), пока человек не вошёл — через Telegram или email.
- ✅ **Возвращён нормальный вход и профиль.** В шапке теперь либо
  кнопка «Войти» (открывает модалку с двумя вкладками — Telegram и
  email/пароль), либо, после входа, чип с именем — при клике открывается
  мини-профиль с последними заказами и кнопкой «Выйти».
- ✅ **Палитра приведена к реальному лого бренда** — голубой → сиреневый
  → розовый неон на почти чёрном фоне (раньше был отдельный
  золотисто-янтарный акцент, не связанный с айдентикой). Лого теперь и
  используется как настоящий brand-mark в шапке/футере/favicon
  (`webapp/assets/logo.png`), а не как отдельно нарисованная SVG-имитация.
- ✅ **Упрощено и оптимизировано.** Убран элемент-имитация «кредитки»
  (чип, наклон карты за курсором на JS, случайный код, дэшед-перфорация)
  — это было визуально эффектно, но избыточно сложно и не связано с
  реальным лого. Убраны бесконечно крутящиеся анимации, которых было
  сразу несколько на одном элементе (sheen-sweep + float + flicker +
  glow одновременно) — оставлена одна декоративная петля на весь hero.
  Убран тяжёлый фон (шумовая маска + сетка + 3 радиальных градиента) —
  остался один статичный слой градиентов.

## Структура проекта

```
webapp/                    сайт — единственное, что нужно деплоить
  index.html
  assets/
    logo.png                 реальное лого бренда — используется как brand-mark и favicon
  css/
    tokens.css                цвет (палитра снята с лого), типографика, отступы — источник правды
    base.css                  сброс, фон (один статичный слой), типографика, focus-стили
    components.css             кнопки, навигация, профиль, модалка входа, тост, бейджи
    sections.css                hero, шаги, каталог с блокировкой, футер, адаптив
    animations.css              keyframes, scroll-reveal, hero-последовательность (немного, осознанно)
  js/
    config.js                  ключи Supabase, ссылки, юзернейм бота
    state.js                   состояние (текущий пользователь)
    data/catalog.js             список товаров + ссылки на оплату
    lib/
      supabaseClient.js
      telegramAuth.js           Telegram Login Widget + серверная проверка входа
      auth.js                    вход/регистрация по email+паролю
      orders.js                  тихий лог последних 5 заказов (для будущего Mini App)
      scrollReveal.js             IntersectionObserver-обёртка для анимаций
    ui/
      catalogView.js              рендер каталога + блокировка до входа
      authModal.js                 модалка входа (вкладки Telegram / email)
      profileMenu.js                чип профиля в шапке + мини-история заказов
      checkoutModal.js             окно оплаты (iframe + «открыть в новой вкладке»)
      docsModal.js                  Политика/Оферта
      toast.js
    content/legalDocs.js           тексты Политики и Оферты (шаблон, показать юристу)
    main.js                         точка входа, склеивает всё вместе

supabase/
  functions/
    _shared/telegram.ts            проверка подписи Telegram Login Widget
    telegram-auth/                  вход/регистрация через Telegram (использует сайт)
    telegram-link/                  привязка Telegram к текущему аккаунту
                                     (пригодится для экрана профиля в Mini App)
bot/
  main.py, config.py, keyboards.py, handlers/start.py
  (Telegram-бот — точка входа в будущий Mini App; сайта не касается)
```

`supabase/` и `bot/` — общий бэкенд для сайта и будущего Mini App,
поэтому оставлены как есть.

## Синхронизация с будущим Mini App

Когда будешь собирать Mini App отдельным проектом, чтобы данные не
разъехались:

1. **Один и тот же Supabase-проект.** Возьми `SUPABASE_URL` и
   `SUPABASE_ANON_KEY` из `webapp/js/config.js` — тогда вход (Telegram
   или email) и сайт создают/находят один и тот же аккаунт.
2. **Один и тот же каталог.** Держи `webapp/js/data/catalog.js`
   синхронным с каталогом в Mini App (те же `id`, `checkoutUrl`, цены).
3. **Формат истории заказов.** Сайт пишет последние 5 заказов в
   `user_metadata.orders` аккаунта (см. `js/lib/orders.js`) в простом
   формате `{ id, name, icon, price, date }`. Mini App может читать это
   же поле — не нужна отдельная таблица.
4. **Палитра.** Токены в `webapp/css/tokens.css` (голубой/сиреневый/
   розовый на почти чёрном, снято с `assets/logo.png`) — используй те же
   переменные и в Mini App, чтобы не было ощущения двух разных продуктов.

## Что нужно донастроить перед запуском

### 1. Ссылки на оплату товаров
1. Заведи товары в своей платёжной системе с ценой, уже включающей
   твою наценку, и получи ссылку на страницу оплаты каждого товара.
2. Вставь эти ссылки в `webapp/js/data/catalog.js`, поле `checkoutUrl`,
   для каждого товара — пока там `null`, кнопка показывает бейдж «скоро».

**Важно:** некоторые платёжные страницы запрещают показ себя во фрейме
(X-Frame-Options/CSP) — тогда покупатель нажмёт «Открыть в новой
вкладке» в окне оплаты. Это нормально, проверяется на реальных товарах.

### 2. Вход через Telegram — требует одноразовой настройки на сервере
Email/пароль работает «из коробки» (это штатная функция Supabase Auth).
А вот вход через Telegram проверяется не в браузере (токен бота нельзя
доверять клиентскому коду), а в Supabase Edge Function — **пока она не
задеплоена, кнопка Telegram в модалке входа будет закономерно выдавать
ошибку «Не удалось связаться с сервером входа через Telegram»**. Это
не баг фронтенда, а недостающий шаг инфраструктуры:

1. **Домен сайта.** У @BotFather → выбери бота → `/setdomain` → укажи
   домен, на котором будет висеть `webapp/`. Без этого виджет Telegram
   Login не заработает.
2. **Деплой Edge Function.**
   ```bash
   supabase functions deploy telegram-auth
   ```
3. **Секреты функции.** В Supabase Dashboard → Edge Functions → Secrets
   пропиши:
   - `BOT_TOKEN` — тот же токен, что в `bot/config.py`/`.env`.
   - `SUPABASE_SERVICE_ROLE_KEY` — Settings → API → service_role key
     (⚠️ секретный ключ, никогда не должен попасть в `webapp/`).
4. **Конфиг фронтенда.** В `webapp/js/config.js` проверь `BOT_USERNAME` —
   юзернейм бота без `@`.

Пока не задеплоено — просто говори покупателям входить по email, это
не блокирует запуск сайта.

### 3. Bot
Токен бота в `bot/config.py` — тестовый, перед публичным запуском
обязательно отзови его через @BotFather → `/revoke` и впиши новый в
`.env` (см. `bot/.env.example`). `SHOP_URL` бота стоит направить на
будущий Mini App, когда он будет готов.

## Честная оговорка про историю заказов

У сайта нет своей таблицы заказов и нет вебхука от платёжной системы,
поэтому запись в `user_metadata.orders` — это лог момента, когда
пользователь нажал «Купить» и открылось окно оплаты, а не подтверждение
того, что оплата прошла успешно. Настоящий статус заказа видит только
платёжная система. Если понадобится показывать реальный статус оплаты —
нужен вебхук от провайдера и отдельная запись о заказе.

## Локальный запуск

`webapp/` — статические файлы, достаточно любого HTTP-сервера:
```bash
npx serve webapp
```
или деплой на Vercel/Netlify/Cloudflare Pages — просто указывай
`webapp/` как корень сайта.

`bot/` — Python 3.11+, `pip install -r requirements.txt`, затем
`python main.py`.
