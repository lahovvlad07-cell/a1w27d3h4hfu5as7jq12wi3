// ===== ОБЩЕЕ СОСТОЯНИЕ САЙТА =====
// Единый объект, на который ссылаются остальные модули. Не создавайте
// свои `let state = ...` в других файлах — только читайте/пишите
// свойства этого объекта, иначе изменения не будут видны всем модулям.
export const state = {
    user: null, // объект пользователя Supabase Auth (или null, если не авторизован)
};
