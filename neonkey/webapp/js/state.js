// ===== ОБЩЕЕ СОСТОЯНИЕ ПРИЛОЖЕНИЯ =====
// Единый объект состояния, на который ссылаются все модули.
// Важно: модули не должны делать `let state = ...` у себя — только
// изменять свойства этого объекта (state.user = ...), иначе изменения
// не будут видны в других файлах.
//
// Баланса/заказов/настроек курсов в состоянии больше нет — их вела
// собственная база (users/orders/settings), а теперь всю продажу и
// доставку делает Digiseller, так что здесь остались только сессия
// пользователя (Supabase Auth) и его аватар (хранится в user_metadata
// того же Auth-аккаунта, отдельная таблица для этого не нужна).
export const state = {
    user: null, // объект пользователя Supabase Auth (или null, если не авторизован)
    avatar: '👤',
};
