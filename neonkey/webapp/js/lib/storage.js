// ===== ЗНАЧЕНИЯ ПО УМОЛЧАНИЮ =====
// Локальное хранилище (localStorage) для данных пользователя и согласия
// намеренно убрано: единственный источник правды — Supabase. Раньше
// localStorage использовалось как резервная копия/кэш, но ключи не были
// привязаны к конкретному Telegram user_id — на одном устройстве/браузере
// данные разных аккаунтов могли перезаписывать друг друга. Эта функция —
// просто фабрика значений по умолчанию, ничего никуда не сохраняет.
export function getDefaultData() {
    return { avatar: '👤', orders: [], balance: 0 };
}
