// ===== ОБЩЕЕ СОСТОЯНИЕ ПРИЛОЖЕНИЯ =====
// Единый объект состояния, на который ссылаются все модули.
// Важно: модули не должны делать `let state = ...` у себя — только
// изменять свойства этого объекта (state.user = ...), иначе изменения
// не будут видны в других файлах.
//
// Собственного баланса и настроек курсов в состоянии нет — приём оплаты
// и доставку товара выполняет подключённая платёжная система. Здесь
// остаются сессия пользователя (Supabase Auth), его аватар и последние
// заказы — всё это хранится в user_metadata того же Auth-аккаунта
// (см. lib/orders.js), отдельные таблицы для этого не нужны.
export const state = {
    user: null, // объект пользователя Supabase Auth (или null, если не авторизован)
    avatar: '👤',
};
