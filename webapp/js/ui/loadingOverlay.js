// ===== ЭКРАН ЗАГРУЗКИ =====
// Скрывается только один раз — когда мы точно знаем, что показывать
// дальше (согласие или сразу приложение). Это устраняет "мигание"
// окна согласия, которое раньше было видно по умолчанию (без класса
// hidden) на время загрузки профиля из Supabase.
export function hideLoadingOverlay() {
    document.getElementById('loadingOverlay')?.classList.add('hidden');
}
